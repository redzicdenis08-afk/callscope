# Launch posts

Use these once after repository topics, preview image, and profile pin are set.

## Show HN

Title:

```text
Show HN: callscope, a tiny benchmark and CLI for AI voice-agent call QA
```

Body:

```text
I built callscope, a zero-runtime-dependency Python CLI for scoring AI voice-agent transcripts.

It separates real human conversations from voicemail, IVR, and no-answer calls, then detects customer signals like pricing questions, callback requests, objections, and booked appointments.

It includes fictional examples, CSV/JSONL exports, and a 20-case benchmark so parser changes have a public yardstick.

Repo: https://github.com/redzicdenis08-afk/callscope
```

## Reddit

```text
I open-sourced a small Python CLI for AI voice-agent call QA.

It takes VAPI-style JSON or plain text transcripts and outputs:
- human vs voicemail vs IVR vs no-answer
- price / callback / booking / objection signals
- 0-100 score
- CSV and JSONL exports
- a 20-case fictional benchmark

No runtime dependencies, examples are fake, runs locally.

Repo: https://github.com/redzicdenis08-afk/callscope
```

## LinkedIn

```text
I open-sourced callscope.

It is a small offline Python tool for teams building AI voice agents. Paste transcript files in, get outcome labels, customer signals, quality scores, CSV/JSONL exports, and a benchmark report out.

Why I built it: provider dashboards can say a call connected, but that does not mean a human stayed, asked about price, requested a callback, or booked.

Repo: https://github.com/redzicdenis08-afk/callscope
```

## X

```text
I open-sourced callscope.

Tiny Python CLI for AI voice-agent transcript QA:
- human vs voicemail vs IVR
- price / callback / booking / objection signals
- CSV + JSONL export
- 20-case fictional benchmark
- zero runtime dependencies

https://github.com/redzicdenis08-afk/callscope
```

## Rule

Do not ask friends to upvote. Do not spam repeats. Post once, then respond to real comments fast.
